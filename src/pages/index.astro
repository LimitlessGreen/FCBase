---
import MainLayout from "@/layouts/MainLayout.astro";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/Card";
import { Button } from "@/components/ui/Button";

const title = "FCBase – Open Flight Controller Database";
const description = "Search and compare ArduPilot, PX4, and iNAV flight controllers – open, community-driven.";

const filters = [
  { label: "MCU", value: "STM32H743" },
  { label: "Form Factor", value: "35x35" },
  { label: "Firmware", value: "ArduPilot" },
];

const controllers = [
  {
    name: "Matek H743",
    slug: "matek-h743",
    mcu: "STM32H743",
    ports: "10x UART, 2x CAN, MicroSD",
    badges: ["CAN", "SD", "UART6"],
    imageAlt: "Placeholder image for Matek H743 flight controller",
  },
  {
    name: "Pixhawk 6C",
    slug: "pixhawk-6c",
    mcu: "STM32H743",
    ports: "12x PWM, 2x CAN, SD",
    badges: ["CAN", "SD", "UART6"],
    imageAlt: "Placeholder image for Pixhawk 6C flight controller",
  },
  {
    name: "Kakute H743 Wing",
    slug: "kakute-h743-wing",
    mcu: "STM32H743",
    ports: "8x UART, 2x CAN, MicroSD",
    badges: ["CAN", "SD", "UART6"],
    imageAlt: "Placeholder image for Kakute H743 Wing flight controller",
  },
];

const features = [
  {
    title: "Comprehensive Database",
    description: "Access detailed specifications for hundreds of flight controllers from major manufacturers.",
    icon: "database",
  },
  {
    title: "Multi-Firmware Support",
    description: "Compare boards compatible with ArduPilot, PX4, and iNAV firmware platforms.",
    icon: "layers",
  },
  {
    title: "Open Source",
    description: "Community-driven with open data under CC-BY 4.0. Contribute and help grow the database.",
    icon: "github",
  },
];
---

<MainLayout title={title} description={description}>
  <!-- Hero Section -->
  <section class="w-full bg-gradient-to-b from-background to-muted/20">
    <div class="container py-16 md:py-24">
      <div class="flex flex-col items-center text-center gap-6 max-w-3xl mx-auto">
        <div class="inline-flex items-center gap-2 rounded-full border px-4 py-2 text-sm">
          <span class="font-medium text-primary">Open Hardware Index</span>
        </div>
        <h1 class="text-4xl font-bold tracking-tight sm:text-5xl md:text-6xl lg:text-7xl">
          FCBase
        </h1>
        <p class="text-xl text-muted-foreground max-w-2xl">
          The open database for flight controllers: compare ArduPilot-, PX4-, and iNAV-compatible boards by MCU, ports, and features.
        </p>
        <div class="flex flex-wrap gap-4 justify-center mt-4">
          <Button size="lg" asChild client:load>
            <a href="/controllers">Browse Controllers</a>
          </Button>
          <Button variant="outline" size="lg" asChild client:load>
            <a href="/contribute">Contribute</a>
          </Button>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="w-full py-16 md:py-24">
    <div class="container">
      <div class="grid gap-8 md:grid-cols-3">
        {features.map((feature) => (
          <Card key={feature.title} client:load>
            <CardHeader>
              <CardTitle>{feature.title}</CardTitle>
              <CardDescription>{feature.description}</CardDescription>
            </CardHeader>
          </Card>
        ))}
      </div>
    </div>
  </section>

  <!-- Search Section -->
  <section class="w-full bg-muted/40 py-16 md:py-24">
    <div class="container">
      <div class="flex flex-col gap-8 max-w-4xl mx-auto">
        <div class="flex flex-col gap-3 text-center">
          <h2 class="text-3xl font-bold tracking-tight sm:text-4xl">
            Find your flight controller
          </h2>
          <p class="text-lg text-muted-foreground">
            Quickly search by MCU, form factor, or supported firmware
          </p>
        </div>

        <Card client:load>
          <CardContent class="pt-6">
            <div class="relative">
              <div class="absolute left-3 top-1/2 -translate-y-1/2">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" class="h-5 w-5 text-muted-foreground">
                  <path stroke-linecap="round" stroke-linejoin="round" d="m21 21-4.35-4.35m1.35-4.15a6.5 6.5 0 1 1-13 0 6.5 6.5 0 0 1 13 0Z" />
                </svg>
              </div>
              <input
                type="search"
                name="q"
                placeholder="Search controllers, MCUs, ports..."
                class="w-full rounded-sm border border-input bg-background px-10 py-3 text-sm ring-offset-background placeholder:text-muted-foreground focus-visible:outline-hidden focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"
                disabled
              />
              <span class="absolute right-3 top-1/2 -translate-y-1/2 text-xs font-medium text-muted-foreground">Coming soon</span>
            </div>
          </CardContent>
        </Card>

        <div class="flex flex-wrap gap-3 justify-center">
          {filters.map((filter) => (
            <Button variant="secondary" size="sm" key={filter.label} client:load>
              <span class="text-xs uppercase tracking-wide text-muted-foreground mr-2">{filter.label}</span>
              {filter.value}
            </Button>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Controllers Grid -->
  <section class="w-full py-16 md:py-24">
    <div class="container">
      <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
        {controllers.map((controller) => (
          <a
            href={`/controllers/${controller.slug}`}
            class="group"
            key={controller.slug}
          >
            <Card client:load class="h-full transition-all hover:shadow-lg hover:-translate-y-1">
              <div class="relative aspect-video w-full bg-gradient-to-br from-muted via-background to-muted overflow-hidden rounded-t-xl">
                <span class="absolute inset-0 flex items-center justify-center px-6 text-center text-sm font-medium text-muted-foreground">
                  {controller.imageAlt}
                </span>
              </div>
              <CardHeader>
                <CardTitle class="group-hover:text-primary transition-colors">
                  {controller.name}
                </CardTitle>
                <CardDescription>{controller.mcu}</CardDescription>
              </CardHeader>
              <CardContent>
                <p class="text-sm text-muted-foreground mb-4">{controller.ports}</p>
                <div class="flex flex-wrap gap-2">
                  {controller.badges.map((badge) => (
                    <span class="inline-flex items-center rounded-sm border bg-secondary px-2.5 py-0.5 text-xs font-semibold uppercase tracking-wide" key={badge}>
                      {badge}
                    </span>
                  ))}
                </div>
              </CardContent>
            </Card>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="w-full bg-primary text-primary-foreground py-16 md:py-24">
    <div class="container">
      <div class="flex flex-col items-center text-center gap-6 max-w-3xl mx-auto">
        <h2 class="text-3xl font-bold tracking-tight sm:text-4xl">
          Ready to contribute?
        </h2>
        <p class="text-lg opacity-90">
          Help us build the most comprehensive flight controller database. Add new boards, update specifications, or improve documentation.
        </p>
        <div class="flex flex-wrap gap-4 justify-center mt-4">
          <Button variant="secondary" size="lg" asChild client:load>
            <a href="https://github.com">View on GitHub</a>
          </Button>
          <Button variant="outline" size="lg" asChild client:load class="bg-primary-foreground/10 hover:bg-primary-foreground/20 text-primary-foreground border-primary-foreground/20">
            <a href="/docs">Read Documentation</a>
          </Button>
        </div>
      </div>
    </div>
  </section>
</MainLayout>

